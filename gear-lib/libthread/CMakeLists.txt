CMAKE_MINIMUM_REQUIRED(VERSION 3.0...3.20)
PROJECT(gear-lib)

INCLUDE_DIRECTORIES(. ${POSIX_INCLUDE_DIR})

LIST(APPEND SOURCE_FILES liblock.c)
LIST(APPEND SOURCE_FILES libthread.c)

ADD_LIBRARY(thread ${SOURCE_FILES})
