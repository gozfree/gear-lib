config LIBFILE_ENABLED
    bool "Enable libfile"
    default n
    if LIBFILE_ENABLED
        config ENABLE_FILEWATCHER
            bool "Enable filewatcher"
            default n
            depends on libdict
            depends on libgevent
            depends on libthread
            depends on libdarray
    endif

