config LIBFILE_ENABLED
    bool "Enable libfile"
    default n
    if LIBFILE_ENABLED
        config ENABLE_FILEWATCHER
            bool "Enable filewatcher"
            default n
            depends on LIBDICT_ENABLED
            depends on LIBGEVENT_ENABLED
            depends on LIBTHREAD_ENABLED
            depends on LIBDARRAY_ENABLED
    endif

